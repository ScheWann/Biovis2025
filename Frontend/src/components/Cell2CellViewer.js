import React, { useRef, useState, useEffect } from 'react';

export const Cell2CellViewer = () => {
    return (
        <div style={{ height: '33%', width: '100%', display: 'flex', justifyContent: 'center', alignItems: 'center', borderBottom: '2px solid #e8e8e8' }}>
            Cell2CellViewer
        </div>
    );
};